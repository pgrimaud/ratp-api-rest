services:
  # CORE SERVICES
  api.storage:
    class: ApiBundle\Services\StorageService
    calls:
      - [ addService, [ 'cache', '@cache.app' ] ]

  # API SERVICES
  api.traffic:
    class: ApiBundle\Services\TrafficService